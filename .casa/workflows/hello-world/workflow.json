{
    "name": "ml-things",
    "description": "doing ml things on a schedule",
    "cmd": "pip install -r requirements.txt > /dev/null && python main.py",
    "schedule": "0 0 * * *",
    "use_gpu": "true"
}